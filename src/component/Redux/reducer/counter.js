import { Abc } from "@mui/icons-material";

export const initState = {
    number : 1,
    reactLink: 'Abc.xyz'
}

const reuducer = (action)=>{
    switch(action.type){
        case INCREASE:{
                
        }
    }
}